{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo Installing frontend deps... && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm run serve",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30000,
    "restartPolicyType": "ON_FAILURE"
  },
  "nixpacks": {
    "nodejs": {
      "version": "18"
    }
  }
}
