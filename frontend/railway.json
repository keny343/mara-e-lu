{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run build && npx serve -s build -l 8080",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30000,
    "restartPolicyType": "ON_FAILURE"
  }
}
