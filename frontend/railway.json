{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm ci --only=production && npm run build && npx serve -s build -l 8080",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30000,
    "restartPolicyType": "ON_FAILURE"
  }
}
